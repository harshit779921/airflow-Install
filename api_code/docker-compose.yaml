services:
  api:
    build: .
    container_name: fastapi-app
    ports:
      - "5000:5000"
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge
